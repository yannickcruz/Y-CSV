import "../css/Editor.css"

const Editor = () => {
    return(
        <section id="Editor">
            <header id="editor-header">
                <h1 id="editor-title">Y-CSV</h1>
            </header>

            <div id="CSV-table">
                {/*Tabela estática apenas para teste. Ao finalizar, implementar tabela gerada por código*/}
            </div>
        </section>
    )
}

export default Editor